#!/bin/bash

sudo apt update && sudo apt upgrade -y

sudo apt install docker.io && sudo apt instal docker-compose -y         #NOTA: vamos a ver en su pagina oficial que esto no se instala asi como lo pongo, consejo: ir a la documentacion oficial siempre
